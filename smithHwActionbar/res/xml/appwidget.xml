<!-- App Widget Provider Metadata file
http://www.vogella.com/articles/AndroidWidgets/
The smallest update interval is 1800000 milliseconds (30 minutes).
http://developer.android.com/reference/android/appwidget/AppWidgetProviderInfo.html#updatePeriodMillis
All long running operations in these methods should be performed in a service, as the execution time for a 
broadcast receiver is limited. Using asynchronous processing in the onReceive() method does not help as the 
system can kill the broadcast process after his onReceive() method.
 -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="150dp"
    android:minHeight="120dp"
    
    android:updatePeriodMillis="10000"
    android:initialLayout="@layout/main_widget"
    android:configure="com.examples.todolist.ConfigureBDayWidgetActivity"
    android:label="My widget"
    android:resizeMode="horizontal|vertical"
    >
</appwidget-provider>
